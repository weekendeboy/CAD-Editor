const fs = require('fs');

let code = fs.readFileSync('src/store/cadStore.ts', 'utf-8');

// I will just ask an AI or do regex? It's easier to write a script that processes the file line by line.
// Wait, I can do it manually or via a quick script.
